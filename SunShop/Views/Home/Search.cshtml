@using SunShop.Models
@using System.Globalization
@model IEnumerable<SunShop.Models.Product>

@{
    ViewBag.Title = "Search";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}



<div class="container px-lg-5">
    <h2 >Search</h2>
    <div class="list-product d-flex flex-column">
        <div class="product-header d-flex justify-content-between">
           
            <div class="d-flex gap-3">
                <button class="btn-prev"><i class="fa-solid fa-angle-left"></i></button>
                <button class="btn-next"><i class="fa-solid fa-angle-right"></i></button>
            </div>
        </div>
        <div class="main-product mt-3">
            @if (Model != null && Model.Any())
            {
                <div class="row g-5">
                    @foreach (Product product in Model)
                    {
                        <div class="col-md-3 col-sm-12">
                            <div class="card position-relative">
                                <a href="/Product/Detail/@product.Id" class="d-block w-100">
                                    <img src="~/Images/@product.ImgUrl" class="card-img-top img-product" alt="...">
                                </a>
                                <div class="card-body">
                                    <div class="d-flex align-items-center gap-1 ratting-container">
                                        <i class="fa-solid fa-star"></i>
                                        <i class="fa-solid fa-star"></i>
                                        <i class="fa-solid fa-star"></i>
                                        <i class="fa-solid fa-star"></i>
                                        <i class="fa-solid fa-star"></i>
                                        <span>(0)</span>
                                    </div>
                                    <h5 class="card-title mb-4">@product.Name</h5>
                                    <div class="d-flex gap-3 justify-content-between align-items-center">
                                        <span class="card-price">@string.Format(CultureInfo.GetCultureInfo("de-DE"), "{0:N0}", product.Price) đ</span>
                                        <button href="/" class="btn btn-primary btn-add-to-cart" data-id="@product.Id">
                                            <i class="fa-solid fa-bag-shopping"></i>
                                        </button>
                                    </div>
                                </div>

                                <button class="position-absolute btn-add-wish">
                                    <i class="fa-solid fa-heart"></i>
                                </button>
                            </div>
                        </div>
                    }



                </div>
            }
            else
            {
                <h1>Không Tìm Thấy Sản Phẩm Nào ?</h1>
            }
        </div>
    </div>
</div>
